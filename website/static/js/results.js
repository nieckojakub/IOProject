// global variables
let searchResult;

//for test purposes
/*
// run code after loading the page
$(function (){
    let token = window.location.pathname;
    token = token.replace('/results/', '');

    $.ajax({
        url: "/search/" + token,
        type: 'GET',
        success: function(data){
            searchResult = data;
            // for testing
            $('#test').html(searchResult);
        },
        error: function() {
            // redirect
            window.location.replace(window.location.origin);
        }
    });
});
*/
// functions
